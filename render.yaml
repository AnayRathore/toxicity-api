services:
  - type: web
    name: toxicity-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn toxicity_api:app --host 0.0.0.0 --port $PORT"
    plan: free
